<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.server.dao.ListSongMapper">

  <!--  返回歌单里指定歌单ID的歌曲-->
  <select id="listSongOfSongId" resultType="ListSong">
    select *
    from list_song
    where song_list_id = #{songListId}
  </select>
  <!--  删除歌单里的歌曲-->
  <delete id="deleteListSong">
    delete from list_song
    where song_id = #{song_id}
  </delete>
<!--  添加歌单的歌曲-->
  <insert id="insertSelective" parameterType="ListSong">
    insert into list_song
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        id,
      </if>
      <if test="songId != null">
        song_id,
      </if>
      <if test="songListId != null">
        song_list_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null"> #{id},
      </if>
      <if test="songId != null"> #{songId},
      </if>
      <if test="songListId != null"> #{songListId},
      </if>
    </trim>
  </insert>
</mapper>
